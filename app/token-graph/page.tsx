import TokenGraph from "@/components/token-graph/TokenGraph";

export const metadata = {
  title: "Token Graph · Design System",
  description: "Visualise the full primitive → semantic → component token hierarchy",
};

export default function TokenGraphPage() {
  return <TokenGraph />;
}
